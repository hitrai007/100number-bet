module.exports = {
  autoApplyChanges: true,  // Automatically apply code changes
  editorIntegration: true, // Ensure Cursor is linked with your editor
  autoSaveInterval: 5000,  // Save changes every 5 seconds
  // Add more configurations here if needed
};

